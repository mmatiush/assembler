#include "op.h"
#include "libft.h"

int		parse_header()
{
	char	*buf;
	int		nu_line;
	int		fd;

	fd = open( && )
	buf = NULL;
	nu_line = 0;
	get_next_line()
	{
		num_line++;
		rm_spaces(str);
		if(str && str[0] == '#')
			read_name_comment(str, &num_line);
	}

	free(str);	
}